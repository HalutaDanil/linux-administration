# Task 5 — Sudo Permissions

Granted sudo privileges to the newly created user.

![Sudo Permissions](../image_for_md/task5.png)

Adding user to `sudo` group with `sudo usermod -aG sudo newuser`

> `sudo` temporarily grants superuser privileges for administrative tasks.
