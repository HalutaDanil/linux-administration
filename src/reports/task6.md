# Task 6 — Time Synchronization

Enabled automatic time synchronization using NTP.

![Time Sync](../image_for_md/task6.png)

Configuring NTP with `sudo timedatectl set-ntp true` and verifying with `timedatectl` and `date`.
